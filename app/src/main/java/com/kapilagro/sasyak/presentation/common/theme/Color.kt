package com.kapilagro.sasyak.presentation.common.theme

import androidx.compose.ui.graphics.Color

// Primary Colors (Agro-based theme)
val AgroPrimary = Color(0xFF6200EE) // purple_500 from colors.xml
val AgroSecondary = Color(0xFF3700B3) // purple_700 from colors.xml
val AgroAccent = Color(0xFFBB86FC) // purple_200 from colors.xml
val AgroLight = Color(0xFFF3EFFE) // Light purple for backgrounds
val AgroDark = Color(0xFF1A0066) // Darker purple for contrast
val AgroMuted = Color(0xFFEDE7F6) // Muted purple for secondary containers
val AgroMutedForeground = Color(0xFF5C5470) // Muted foreground for text
val White = Color(0xFFFFFFFF)

// UI Colors
val Background = White
val Foreground = AgroDark
val Border = Color(0xFFD1C4E9) // Light purple-gray for borders
val Card = Color(0xFFFFFFFF)
val Input = Color(0xFFF5F0FF) // Very light purple for inputs
val Surface = Color(0xFFFDFEFD) // Pristine surface color


// Status Colors
val Success = Color(0xFF03DAC5) // teal_200 from colors.xml
val Warning = Color(0xFFFFB300) // Bold amber for warnings
val Error = Color(0xFFD81B60) // Bold pink-red for errors
val Info = Color(0xFF0288D1) // Bold blue for info

// Task Status Colors
val StatusApproved = Color(0xFF03DAC5) // teal_200
val StatusPending = Color(0xFFFFB300) // Amber
val StatusRejected = Color(0xFFD81B60) // Pink-red
val Green500 = Color(0xFF6200EE) // Matches purple_500

// Supervisor Quick Action Colors
val ScoutingIcon = Color(0xFF6200EE) // Purple (purple_500)
val ScoutingContainer = Color(0xFFF3EFFE) // Light purple background
val SprayingIcon = Color(0xFF03DAC5) // Teal (teal_200)
val SprayingContainer = Color(0xFFE0F7FA) // Light teal background
val SowingIcon = Color(0xFFBB86FC) // Light purple (purple_200)
val SowingContainer = Color(0xFFF5F0FF) // Very light purple background
val FuelIcon = Color(0xFFFFB300) // Amber
val FuelContainer = Color(0xFFFFF8E1) // Light amber background
val YieldIcon = Color(0xFF0288D1) // Blue
val YieldContainer = Color(0xFFE3F2FD) // Light blue background

// Submitted Status Colors
val SubmittedContainer = Color(0xFFEDE7F6) // Muted purple
val SubmittedText = Color(0xFF3700B3) // purple_700

// Approved Status Colors
val ApprovedContainer = Color(0xFFE0F7FA) // Light teal
val ApprovedText = Color(0xFF018786) // teal_700

// Rejected Status Colors
val RejectedContainer = Color(0xFFFDE4EA) // Light pink-red
val RejectedText = Color(0xFFD81B60) // Pink-red

// Manager Quick Action Colors
val TeamIcon = Color(0xFF6200EE) // Purple (purple_500)
val TeamContainer = Color(0xFFF3EFFE) // Light purple background
val ReportsIcon = Color(0xFF03DAC5) // Teal (teal_200)
val ReportsContainer = Color(0xFFE0F7FA) // Light teal background
val TasksIcon = Color(0xFFBB86FC) // Light purple (purple_200)
val TasksContainer = Color(0xFFF5F0FF) // Very light purple background
val AdviceIcon = Color(0xFFFFB300) // Amber
val AdviceContainer = Color(0xFFFFF8E1) // Light amber background

// Additional dark mode colors
val BackgroundDark = Color(0xFF121212)
val SurfaceDark = Color(0xFF1E1E1E)
